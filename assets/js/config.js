/* Shark.Bet site configuration — edit here, no rebuild needed. */
window.SB_CONFIG = {
  siteName: "Shark.Bet",
  domainInquiryUrl: "https://web.works/contact",
  // Google AdSense publisher ID, e.g. "ca-pub-1234567890123456". Leave empty until approved.
  adsenseClient: "",
  // Contact channel is kept encoded; it is only assembled at the moment a form is sent
  // or an "Email us" link is clicked. Never paste it in plain text anywhere in the site.
  _k: ["bW9jLm", "xpYW1n", "QDFhc2", "tyb3di", "ZXc="],
  // Donation currency for the PayPal donate flow
  currency: "USD",
  // YouTube channel handle for the video hub (optional)
  youtubeChannel: "https://www.youtube.com/results?search_query=sports+betting+strategy+expected+value"
};
